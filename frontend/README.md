# TaskFlow frontend

Nuxt 4 SSR client for TaskFlow. Use the repository root [README](../README.md) for setup and commands. Run `pnpm lint`, `pnpm typecheck`, `pnpm test`, and `pnpm build` before delivery.
